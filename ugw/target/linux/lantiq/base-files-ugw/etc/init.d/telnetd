#!/bin/sh

#start() {
#	/usr/sbin/telnetd 2>&1 &
#}
#
#stop() {
#	killall telnetd 
#}

case "$1" in
	start)
		#start
		/usr/sbin/telnetd 2>&1 &
		;;
	stop)
		#stop
		killall telnetd 
		;;
esac

#exit $?
