config LTQ_GPIO_LED
   bool "Lantiq GPIO LED configure script"
   depends on PACKAGE_kmod-leds-gpio
   default y
   help
       This is a quite simple script to configure the (NETCORE) board GPIO LEDS
